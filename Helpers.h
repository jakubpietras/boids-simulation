#pragma once
#include <cstdlib>

class Helpers
{
public:
	static float randomFloat(float minValue, float maxValue);
	static float vectorNorm(float compX, float compY);
	//static float calculateDistance(float fromX, float fromY, float toX, float toY);
};

